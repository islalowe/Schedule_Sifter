#plain text file to keep environment variables (settings and secrets).
# Instead of hard-coding passwords/API keys, etc., they go in .env, and the app loads them with the dotenv package at startup.

PORT=4000
MONGO_URI=mongodb+srv://...
SESSION_SECRET=replace_me
APP_BASE_URL=http://localhost:4000
CLIENT_ORIGIN=http://localhost:5173
FLASK_URL=http://127.0.0.1:5000

GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...
GOOGLE_CALLBACK_URL=http://localhost:4000/auth/google/callback

# 32-byte base64 key for AES-GCM (to encrypt refresh tokens etc.)
DATA_KEY_BASE64=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx=
